{
  "name": "ec0-root",
  "version": "1.0.0",
  "description": "Root configuration for EcoMarket monorepo",
  "scripts": {
    "install:all": "cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "build:full": "npm run install:all && npm run build:frontend",
    "build": "npm run build:full",
    "start": "node backend/server.js"
  }
}
